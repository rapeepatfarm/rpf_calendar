# RPF Calendar — Project Context for Claude

ปฏิทินวางแผนและติดตามงานฟาร์ม · โปรแกรมที่ 5 ของ RPF Systems
แผนฉบับเต็มอยู่ที่ `D:\RPF_Systems\rpf calendar.md`

| | |
|---|---|
| Stack | Python 3.12 · FastAPI · Jinja2 · Alpine.js · psycopg2 · bcrypt (ไม่มี dependency สำหรับ recurrence) |
| DB | PostgreSQL `rpf_calendar` (แยกจาก DB อื่น) |
| Port | 8200 (feed 5000 · medicine 5001 · farm 8000 · power 8100) |
| รัน | `start.bat` หรือ `python -m uvicorn main:app --host 0.0.0.0 --port 8200` |
| ธีม | สว่างเป็นค่าเริ่มต้น สลับมืดได้ (ต่างจาก rpf_power ที่มืดเป็นค่าเริ่มต้น) |

โปรแกรมนี้ลอกสถาปัตยกรรมมาจาก `rpf_power` — `config.py` / `database.py` / `auth.py`
โครงสร้างเหมือนกันเกือบทั้งหมด แก้ที่หนึ่งควรพิจารณาแก้อีกที่ด้วย

---

## กฎที่ห้ามละเมิด

### 1. ห้ามให้ระบบแก้ `planned_date` เอง (ยกเว้นตอนกดเริ่มงาน)
งานที่ไม่ได้กดเริ่มจะ "โผล่ในงานวันนี้" ด้วยการ **query** ไม่ใช่การ UPDATE วันที่

```sql
status = 'planned' AND planned_end_date < CURRENT_DATE AND carry_over
```

ถ้าย้ายวันจริงจะเสียประวัติ คิด % ทำตรงแผนไม่ได้ และชนกับ
`UNIQUE (series_id, planned_date)` ของแผนงานประจำ
การเลื่อนวันต้องมาจากคนกดเท่านั้น และเขียน log `rescheduled` เสมอ

**ข้อยกเว้นเดียว (migration 008): การกดเริ่มงาน**
ผู้ใช้สั่งให้ปฏิทินสะท้อนความจริง — กดเริ่มเมื่อไร แผนเลื่อนไปวันนั้น
งานหลายวันเลื่อนทั้งแผง ระยะเวลาไม่เปลี่ยน · ทำใน `_shift_to_actual_start()`
ที่เดียว เรียกจาก `start()` เท่านั้น

ยังไม่ละเมิดเจตนาเดิม เพราะ **คนเป็นคนกด** ไม่ใช่ระบบขยับเอง
งานที่ไม่มีใครแตะยังห้ามขยับเหมือนเดิม — งานค้างยังโผล่ด้วย query ไม่ใช่ UPDATE

วันเดิมถูกเก็บที่ `planned_date_original` / `planned_end_date_original`
เขียนด้วย `COALESCE(เดิม, ค่าใหม่)` จึง **เขียนครั้งแรกครั้งเดียว**
ถ้าเขียนทับทุกรอบ พอมีคนกดเริ่ม-ถอน-เริ่มใหม่ "แผนเดิม" จะกลายเป็นวันที่เพิ่งเลื่อน
แล้วรายงานจะบอกว่าทำตรงแผนทั้งที่ช้าไปหลายวัน

**แผนงานประจำที่เลื่อนแล้วชนรอบอื่น — ถามยืนยันชั้นที่สอง**
`UNIQUE (series_id, planned_date)` ทำให้สองรอบอยู่วันเดียวกันไม่ได้ ถ้าดันทุรัง UPDATE
ทั้งคำสั่งจะพัง แล้วการกดเริ่มงานล้มไปด้วยทั้งที่ผู้ใช้แค่กดเริ่มงาน

ทางออกที่ผู้ใช้เลือก: **เลื่อนรอบถัดไปออกไปตามคาบของแผน** พอรอบถัดไปหลบแล้ว
การชนก็หายไปเอง — ไม่ต้องแก้โครงสร้างฐานข้อมูล และตรงกับความจริงหน้างานด้วย
(เพิ่งเปลี่ยนน้ำมันวันนี้ รอบหน้าก็ควรนับจากวันนี้)

เป็นสองจังหวะโดยตั้งใจ: กดเริ่ม → งานเริ่มจริงทันที แต่**ยังไม่เลื่อนวัน** →
เด้งไปหน้ารายละเอียดพร้อม `?ask_shift=1` → กล่องยืนยันชั้นสองบอกว่ารอบไหนจะขยับไปไหน
→ ยืนยันแล้วค่อยขยับทั้งชุด (`shift_chain()` / `apply_shift()`)

**ธง ask_shift อยู่ใน query string ไม่ได้เก็บสถานะลง DB** — ถ้าผู้ใช้ปิดกล่องไป
ถือว่าไม่เลื่อน จะได้ไม่มีกล่องค้างตามหลอกทุกครั้งที่เปิดหน้านี้ · งานยังทำต่อได้ปกติ
แค่วันในปฏิทินคงเป็นวันเดิม

**ต้อง UPDATE จากปลายลูกโซ่ย้อนกลับมา** — unique index ตรวจทีละคำสั่ง ไม่ได้รอ commit
ถ้าขยับตัวต้นก่อน มันจะไปทับตัวถัดไปที่ยังไม่ได้ขยับ แล้วพังทันที
· ลูกโซ่เกิดได้จริงเพราะรอบที่ถูกดันอาจไปชนรอบถัดไปอีกต่อหนึ่ง

**ถ้าลูกโซ่ไปเจอรอบที่เริ่ม/ปิดไปแล้ว จะไม่ขยับ** แล้วคืนเหตุผลให้แสดงแทน
เพราะการขยับงานที่ลงมือไปแล้วคือการแก้ประวัติ

### 2. router ห้าม UPDATE activities เอง
ทุกการเปลี่ยนแปลงผ่าน `services/activities.py` เพราะเป็นที่เดียวที่เขียน `activity_log`
ถ้ามีทางลัด จะเกิดเส้นทางที่เปลี่ยนสถานะแล้วประวัติหาย

### 3. `actual_minutes` เก็บค่าไว้ ไม่คำนวณสด
คำนวณตอนกดสิ้นสุดแล้วเก็บลงคอลัมน์ ถ้าคำนวณจาก `finished_at - started_at`
ทุกครั้งที่เปิดรายงาน ตัวเลขจะขยับเมื่อมีคนแก้เวลาย้อนหลัง

### 4. `missed` ≠ `cancelled`
`missed` = วางแผนแล้วทำไม่ทัน · `cancelled` = ตั้งใจยกเลิกแผน
รายงานต้องแยกสองอย่างนี้ออกจากกันได้

### 5. JSON ใน attribute ของ Alpine ต้องใช้ single quote
`tojson` ของ Jinja หนี `'` แต่ไม่หนี `"` — เขียน `x-data='{...{{ x | tojson }}...}'`
ถ้าใช้ `"` ครอบ Alpine จะ parse ไม่ผ่านและ modal จะไม่ทำงาน (เคยพลาดมาแล้ว)

### 6. งานหลายวัน
`planned_date` = วันเริ่ม · `planned_end_date` = วันสิ้นสุด (งานวันเดียวเก็บเท่ากัน)
คำถาม "งานไหนอยู่ในช่วงนี้" ต้องเป็น **การคาบเกี่ยว** ไม่ใช่ `BETWEEN`:
`planned_date <= ปลายช่วง AND planned_end_date >= ต้นช่วง`
และ **งานค้าง/`days_late` นับจาก `planned_date`** (วันที่ต้องเริ่ม) ตามที่ผู้ใช้กำหนด
ตัวเลขนี้ตอบว่า "ควรลงมือไปแล้วกี่วันแต่ยังไม่ได้เริ่ม" งานหลายวันจึงขึ้นว่าค้าง
ตั้งแต่วันที่สองของช่วง ซึ่งถูกต้องแล้ว — เลยวันเริ่มมาแล้วยังไม่กด ก็คือช้าจริง

ต้องใช้ฐานเดียวกันทั้ง `activities._SELECT`, `today_groups()` และ
`scheduler.close_stale()` ไม่งั้นป้ายบนจอบอกค้าง 9 วัน แต่ตัวปิดอัตโนมัติ
ที่ตั้งไว้ 7 วันยังไม่ทำงาน แล้วไม่มีใครเข้าใจว่าทำไม

**กลุ่มงานค้างกับกลุ่มงานวันนี้ต้องไม่ทับกัน** — งานหลายวันที่เลยวันเริ่มมาแล้ว
เข้าเงื่อนไขทั้งสองกลุ่มพร้อมกัน `today_groups()` จึงตัดตัวที่อยู่ในกลุ่มค้างออก
จากกลุ่มวันนี้ · เงื่อนไขที่ตัดต้องมี `carry_over` ด้วยให้ตรงกับกลุ่มค้างเป๊ะ
ไม่งั้นงานที่ตั้ง "ไม่ตามมา" จะหายไปจากทั้งสองกลุ่มตั้งแต่วันที่สอง

### 7. ON CONFLICT ของ index บางส่วน
`activities_series_date_uq` เป็น **partial** unique index จึงต้องเขียน predicate ซ้ำ:
`ON CONFLICT (series_id, planned_date) WHERE series_id IS NOT NULL DO NOTHING`
ถ้าไม่ใส่ PostgreSQL จะหา arbiter ไม่เจอแล้วโยน InvalidColumnReference

### 8. นิยามสถิติอยู่ใน reports.py ที่เดียว
`% ทำตรงแผน = ตรงแผน ÷ (ตรงแผน + ช้า + ไม่ได้ทำ)` — **ยกเลิกและงานที่ยังไม่ปิด
ไม่อยู่ในตัวหาร** เพราะการยกเลิกแผนโดยตั้งใจไม่ใช่ความผิดพลาด
ตัวหารจึงไม่เท่ากับคอลัมน์ "ทั้งหมด" ในตาราง — ต้องแสดงเศษ/ส่วนกำกับเสมอ
ไม่งั้นผู้ใช้จะคิดว่าเลขผิด (เช่นทั้งหมด 9 งานแต่ขึ้น 100%)

**ตัดสินตรงแผน/ช้า และเลือกช่วงเดือน จาก `_PLAN_END`**
(`COALESCE(planned_end_date_original, planned_end_date)`) ไม่ใช่คอลัมน์ดิบ
เพราะตั้งแต่ข้อ 1 การกดเริ่มงานเลื่อนวันแผนไปทับความจริงแล้ว
ถ้าตัดสินจากคอลัมน์ที่ถูกเลื่อน **ทุกงานจะเป็น "ตรงแผน" หมด 100% ตลอดกาล**

ใช้กับการเลือกช่วงเดือนด้วย เพื่อให้รายงานเดือนที่ผ่านไปแล้วไม่ขยับเอง
เมื่อมีคนเพิ่งไปกดเริ่มงานเก่า

เทียบวันที่ทำเสร็จกับวันแผนต้องแปลงเป็นวันไทยก่อน
(`finished_at AT TIME ZONE 'Asia/Bangkok'`) ไม่งั้นงานที่กดจบ 6 โมงเย็นจะนับเป็นวันถัดไป

### 9. ผู้ช่วย (activity_helpers)
`attach_helpers()` ต้องเรียก **ก่อน** `annotate()` ทุกครั้ง เพราะ `annotate` คิด
`can_run` จาก `owns_activity` ซึ่งอ่าน `helper_ids` · ถ้าสลับลำดับ ผู้ช่วยจะกดเริ่ม
งานตัวเองไม่ได้และไม่มี error ให้เห็น · `_load()` ใน routers/activities.py
เติมให้แล้วในตัว

### 10. ยืนยันก่อนเริ่มงาน
`start_state()` บอกว่าวันนี้อยู่ตรงไหนของช่วงแผน (early / ontime / late)
คำนวณที่ฝั่งเซิร์ฟเวอร์ที่เดียว **ห้ามคำนวณซ้ำใน JavaScript** ไม่งั้นเกณฑ์สองฝั่ง
จะเพี้ยนกันเมื่อเครื่องผู้ใช้ตั้งวันผิด

`start()` **บังคับ `confirmed=True` เฉพาะกรณี early** เพราะการเริ่มก่อนกำหนดทำให้
ข้อมูลเวลาที่ใช้จริงเพี้ยน จึงต้องกันจริงที่เซิร์ฟเวอร์ · ส่วน ontime/late
กล่องยืนยันเป็นแค่ตัวกันกดพลาดบนหน้าจอ ไม่บังคับที่เซิร์ฟเวอร์
เผื่อเบราว์เซอร์ปิด JavaScript จะยังกดเริ่มงานตามปกติได้

`unstart()` ล้าง `started_at`/`started_by` ทิ้ง เพราะเวลาที่กดผิดไม่ใช่เวลาเริ่มงานจริง
ถ้าเก็บไว้ รายงาน "เวลาที่ใช้จริง" จะยาวเกินความจริง — ประวัติใน `activity_log` ยังอยู่ครบ

**และคืนวันในแผนกลับด้วย** (`_restore_planned_dates()`) ด้วยเหตุผลเดียวกันเป๊ะ —
ถอนการเริ่ม แปลว่า "ไม่ได้เริ่มจริง" วันที่ถูกเลื่อนมาเพราะการกดผิดจึงไม่ใช่วันจริงเหมือนกัน
ถ้าไม่คืน แผนจะถูกย้ายถาวรทั้งที่ไม่มีใครได้ลงมือทำอะไรเลย

ล้าง `planned_date_original` / `planned_end_date_original` ทิ้งด้วย ให้กลับไปเป็น
เหมือนไม่เคยถูกเลื่อน ไม่งั้นรายงานจะยังเทียบกับ "แผนเดิม" ทั้งที่วันปัจจุบันคือแผนเดิมนั้นแล้ว

**ต้องเช็กการชนก่อนคืนวันเสมอ** — ระหว่างที่งานย้ายออกไป รอบอื่นของแผนประจำ
อาจเข้ามาจับจองวันเดิมแล้ว ถ้าคืนไปตรงๆ จะชน `UNIQUE (series_id, planned_date)`
แล้ว**การกดถอนจะพัง** ทั้งที่ผู้ใช้แค่กดถอน · กรณีนั้นถอนสถานะให้ตามปกติ
แต่ไม่คืนวัน พร้อมบันทึกเหตุผลลง log

**และดึงรอบที่ถูกดันออกไปกลับมาด้วย** (`_restore_pushed_rounds()`, migration 009)
กติกาเดียวกันทั้งโปรแกรม: **ระบบย้ายวันไหนไป ก็ย้ายกลับได้หมด** ไม่คืนครึ่งเดียว
ไม่งั้นตารางจะค้างในสภาพที่ไม่มีใครตั้งใจ — งานกลับวันเดิมแล้ว แต่รอบถัดไป
ยังถูกดันออกไปอีก 6 เดือน

ต้องมีสองคอลัมน์คู่กันถึงจะย้อนได้: `planned_date_original` บอก *วันเดิมคือวันไหน*
· `shift_source_id` บอก *ใครเป็นคนทำให้ต้องย้าย*

**ลำดับการคืนสำคัญ** — เรียงตาม `planned_date_original` จากน้อยไปมาก คือไล่จาก
ต้นลูกโซ่ไปหาปลาย ปลายทางของแต่ละตัวถูกปล่อยว่างโดยตัวก่อนหน้าพอดี
(งานตัวเองคืนก่อน จึงปล่อยวันนี้ว่างให้ตัวแรกของลูกโซ่) · สลับลำดับเมื่อไรชน
`UNIQUE (series_id, planned_date)` แล้วพังกลางคัน

**สองกรณีที่ห้ามดึงกลับ** — รอบที่ไม่ใช่ `planned` แล้ว (มีคนกดเริ่ม/ปิดไปแล้ว)
และรอบที่คนไปแก้วันเอง · กรณีหลังตัดสายด้วยการเคลียร์ `shift_source_id`
ใน `update()` เมื่อวันเปลี่ยน — คนแก้วันเองแล้วถือว่าเป็นเจ้าของวันนั้น
ระบบต้องไม่ไปยึดคืน

**แก้วันเริ่มงานย้อนหลัง** (`set_start_date()`) — ใช้ตอนลงมือจริงไปแล้วแต่ลืมกดปุ่ม
ต้องแก้ **สองอย่างพร้อมกันเสมอ** ไม่ใช่อย่างเดียว:
1. `started_at` — `finish()` คิด `actual_minutes` จากค่านี้ ถ้าไม่แก้ เวลาที่ใช้จริงจะสั้นกว่าความจริง
2. วันในแผน — กติกาข้อ 1 บอกว่าปฏิทินต้องสะท้อนวันที่ลงมือจริง

เลื่อนวันแผนผ่าน `_shift_to_actual_start(..., target=วันใหม่)` **ตัวเดียวกับตอนกดเริ่มงาน**
ห้ามเขียนตรรกะเลื่อนวันขึ้นมาใหม่ ไม่งั้นกติกาคงระยะเวลา/เก็บวันเดิมจะแยกร่างแล้วเพี้ยนจากกัน

**เปลี่ยนแค่วัน เก็บเวลานาฬิกาเดิมไว้** — ประกอบใน SQL ด้วย
`(%s::date + (started_at AT TIME ZONE 'Asia/Bangkok')::time) AT TIME ZONE 'Asia/Bangkok'`
ทำใน PostgreSQL เพราะมันมีฐานข้อมูลเขตเวลาของตัวเอง ไม่ต้องพึ่ง Windows (ดูข้อ 14)

ที่นี่**ไม่มีขั้นตอนยืนยันสองชั้น**แบบตอนกดเริ่มงาน — ถ้าวันปลายทางชนรอบอื่นของแผนประจำ
จะปฏิเสธพร้อมบอกเหตุผล เพราะการแก้ย้อนหลังมักขยับแค่วันสองวัน โอกาสชนต่ำมาก
ไม่คุ้มกับความซับซ้อนของกล่องยืนยันอีกชั้น

กล่องยืนยันเป็น Alpine ตัวเดียวใน `base.html` (`startAsk` / `dateAsk`)
ปุ่มทุกที่เรียกตัวเดียวกัน ใช้ macro `start_payload()` / `date_payload()` ประกอบข้อมูล
เพื่อไม่ให้แต่ละหน้าลืมฟิลด์

### 11. โหมดนับรอบของแผนงานประจำ (anchor_mode)
`fixed` (ค่าเริ่มต้น) = ยึดวันตายตัวตามปฏิทิน · คำนวณล่วงหน้า 12 เดือนได้
`after_done` = รอบถัดไป = วันที่ทำเสร็จจริง + คาบ

**โหมด after_done สร้างล่วงหน้าได้ครั้งละ 1 รอบเท่านั้น** เพราะวันของรอบถัดไป
ขึ้นกับว่ารอบปัจจุบันจะเสร็จเมื่อไร ซึ่งยังไม่รู้ · `generate_series()` จึงแยกทาง
ไปเรียก `ensure_next_occurrence()` แทนการวางยาว

รอบใหม่ถูกสร้างตอน**ปิดงาน** ผ่าน `_advance_series()` ใน services/activities.py
เรียกจากทั้ง `finish` / `cancel` / `mark_missed` **และ `close_stale()` ใน scheduler**
— ถ้าลืมที่ใดที่หนึ่ง สายของแผนจะขาดเงียบๆ ไม่มีอะไรเตือน

anchor ที่ใช้: ปิดแบบ done → วันที่ทำเสร็จ · ปิดแบบ cancelled/missed → planned_end_date
(เพราะไม่ได้ลงมือทำ จึงไม่ควรเลื่อนรอบถัดไปตามวันที่กดปิด)

ในโหมดนี้ `bymonthday` / `nth_weekday` **ถูกละไว้โดยตั้งใจ** — ดู `period_after()`
ถ้าดึงกลับไปหาวันตายตัว ระยะห่างที่ผู้ใช้ต้องการจะเพี้ยน

### 12. แก้วันที่ของงานที่ซิงค์มา (date_overridden)
คนหน้างานแก้วันของงานที่ซิงค์มาได้ **ที่ปฏิทินนี้** ไม่ต้องกลับไปแก้ที่โปรแกรมต้นทาง

`activities.update()` เป็นที่เดียวที่ปักธง — ถ้ามี `source_system` แล้ววันเปลี่ยน
จะตั้ง `date_overridden = TRUE` และธงนี้ **ไม่เคยถูกล้างเอง** (เขียนเป็น
`date_changed or ค่าเดิม`) เพราะถ้าล้างเมื่อคนแก้ฟิลด์อื่น รอบซิงค์ถัดไป
จะดึงวันเดิมจากต้นทางมาทับวันที่แก้ไว้ โดยไม่มีใครรู้

`sync/runner._update_plan_only()` เคารพธงนี้ — วันมาจากแถวเดิม ไม่ใช่จากต้นทาง
ส่วนฟิลด์อื่น (ชื่องาน หมายเหตุ) ยังให้ต้นทางเป็นเจ้าของเหมือนเดิม

**ทิศทางข้อมูลไม่เปลี่ยน** — ปฏิทินไม่เขียนลง DB ของฟาร์ม ฟาร์มเป็นฝ่ายมาดึงเอง
ผ่าน `rpf_farm/services/calendar_pull.pull_date_changes()` (ช่องทางเดียวกับที่ดึงผลงาน)
บัญชี `rpf_readonly` จึงยังเป็น SELECT อย่างเดียวได้ตามเดิม

ฝั่งฟาร์มมีธงคู่กันคือ `flock_activity_plans.date_adjusted` ซึ่ง
`services/activity_plan.apply_program()` ต้องเช็กก่อนคำนวณวันใหม่จากอายุไก่
ไม่งั้นสั่ง apply ซ้ำทีเดียววันที่คนแก้หายเงียบ · วันเดิมเก็บไว้ที่
`planned_date_original` เพื่อให้ยังดูออกว่าเลื่อนไปกี่วัน

`pull_date_changes()` ขยับเฉพาะแผนที่ยัง `pending` — แผนที่ปิดไปแล้วนับเป็น `locked`
เพราะการขยับวันของงานที่ทำไปแล้วคือการแก้ประวัติ

migration: `migrations/007_date_override.sql` (ปฏิทิน) + `rpf_farm/migrations/007_date_adjusted.sql`

### 12b. ร่องรอยของรอบซิงค์ที่พัง ต้องเขียนบน connection แยก
`runner.record_failure()` เปิด connection ใหม่ที่ commit ของตัวเอง **ห้ามย้ายกลับ
ไปเขียนบน conn เดิม** — รอบที่พังจะถูก rollback ทั้ง transaction ร่องรอยจะหายไปด้วย
รวมถึงแถว `sync_runs` ที่ INSERT ไว้ตอนเริ่ม

ของเดิมเขียนบน conn เดิม ผลคือผู้ใช้กด "ดึงเดี๋ยวนี้" ไม่สำเร็จหลายรอบ
โดยไม่มีอะไรบันทึกไว้เลยว่าพังเพราะอะไร · รู้ทีหลังได้จาก **เลข sequence ที่ถูกกินไปแล้ว**
เท่านั้น (`sync_runs.id` กระโดดถึง 11 และ `activities.id` กระโดด 118→127
ทั้งที่สองตารางไม่มีแถวใหม่เลย) · sequence ไม่ย้อนกลับตอน rollback จึงเป็นหลักฐานเดียวที่เหลือ

`routers/sync_admin.py` ก็ห้าม `execute()` ต่อบน conn ที่ transaction เสียแล้ว
คำสั่งถัดไปจะโยน `InFailedSqlTransaction` ทับสาเหตุจริง แล้วผู้ใช้จะเห็นหน้า error 500
แทนข้อความบอกสาเหตุ — ให้ `conn.rollback()` ก่อน return เสมอ

### 12c. เมนูต้องพาไปถึงทุกหน้าที่สิทธิ์เปิดให้
`/settings/sync` เคยไม่มีลิงก์ใน sidebar เลย · แถบ "ตั้งค่า" ที่รวมทางเข้าไว้
มีเฉพาะ **แถบล่างของมือถือ** คนใช้เดสก์ท็อปจึงเข้าหน้าซิงค์ไม่ได้นอกจากพิมพ์ URL เอง
เพิ่มหน้าใหม่แล้วต้องเช็กทั้งสองทาง และ **เงื่อนไขที่ซ่อนเมนูต้องตรงกับ
`require_role()` ของ route จริง** (หน้านี้เป็น manager ไม่ใช่ admin)

ปุ่ม "⇄ ซิงค์" บนแถบบนของหน้าปฏิทินยิงไป `/settings/sync/run-all`
ซึ่งวนทุกแหล่งที่เปิดใช้ **แยก transaction ต่อแหล่ง** เพื่อไม่ให้แหล่งที่พัง
ล้มผลของแหล่งที่สำเร็จไปแล้ว

### 12d. ปุ่มย้อนกลับในหน้ารายละเอียด ใช้ session ไม่ใช่ Referer
`view.page()` จำ path ของหน้าหลักล่าสุด (`MAIN_PAGES`) ลง session พร้อม query string
เพื่อให้กดย้อนกลับแล้วได้เดือนและตัวกรองเดิม

**ห้ามเปลี่ยนไปใช้ header `Referer`** — หลังกดเริ่ม/จบงาน หน้าจะ redirect กลับมาที่
รายละเอียดอีกรอบ Referer จะกลายเป็นหน้ารายละเอียดเอง แล้วปุ่มย้อนกลับจะวนอยู่กับที่

`MAIN_PAGES` เทียบ path **ตรงตัว ไม่ใช่ startswith** ไม่งั้นหน้าลูกอย่าง
`/plans/3/edit` หรือ `/activities/5/edit` จะถูกจำเป็นหน้าหลัก แล้วกดย้อนกลับ
จะเด้งเข้าฟอร์มแก้ไขแทนที่จะเป็นหน้ารายการ

### 12e. หน้าปฏิทิน — เดือนอย่างเดียว + แผงข้างสองแถบ
ปุ่มสลับเดือน/สัปดาห์ถูกถอดออกตามที่ผู้ใช้สั่ง (กินพื้นที่แถบบนโดยที่คนใช้ดูเป็นเดือนอย่างเดียว)
`calgrid.build_week()` ยังอยู่ในโค้ดและมีเทสต์คุม แต่**ไม่มีหน้าจอไหนเรียกใช้แล้ว** —
เก็บไว้เผื่ออยากได้กลับมา ถ้าจะลบต้องลบเทสต์ใน `tests/test_reports.py` ด้วย

แผงข้างเปลี่ยนจาก "รายละเอียดของวันที่เลือก" เป็นสามสภาพใน `calendar.js`:
`tab='next'` · `tab='overdue'` · `picked` (รายละเอียดตัวที่กด)

**สองลิสต์ไม่ผูกกับเดือนที่กำลังดูอยู่โดยตั้งใจ** — งานค้างส่วนมากอยู่เดือนก่อนหน้า
ถ้ากรองตามเดือนที่เปิดอยู่ พอเลื่อนไปดูเดือนอื่นงานค้างจะหายไปเฉยๆ
router จึงส่ง `next_json` / `overdue_json` แยกจาก `items_json` (ซึ่งเป็นของเดือนนั้นจริงๆ)

`find()` ใน calendar.js ค้นทั้งสามชุด เพราะแถบที่กดบนปฏิทินอาจไม่ได้อยู่ในลิสต์ใดเลย
(งานที่ปิดไปแล้ว หรืองานที่ไกลเกิน `PANEL_AHEAD_DAYS`) ถ้าค้นแค่ลิสต์ กดแล้วจะไม่ขึ้นอะไร

**กดจากลิสต์แล้วปฏิทินเลื่อนตาม** — ลิสต์มองไปข้างหน้า 30 วัน จึงมีของเดือนถัดไปปนอยู่
`pick()` เช็กว่ากิจกรรมนั้นอยู่ใน `items` (ของที่วาดบนตารางจริงๆ) หรือไม่
ถ้าไม่อยู่ก็โหลดหน้าใหม่ไปเดือนนั้นพร้อม `?a=<id>` แล้วกางรายละเอียดต่อ

**เช็กจาก `items` ห้ามเทียบเลขเดือน** — ตารางเดือนมีวันของเดือนข้างเคียงติดมาเสมอ
งานวันที่ 1 ก.ย. มองเห็นอยู่แล้วในตารางเดือน ส.ค. ถ้าเทียบเลขเดือนจะเด้งทั้งที่ไม่ต้องเด้ง

`?a=` ใช้ครั้งเดียวตอนโหลดหน้า · `syncUrl()` ลบทิ้งเมื่อผู้ใช้ปิดรายละเอียด
ไม่งั้นกดรีเฟรชแล้วรายละเอียดจะเด้งกลับมาเองทั้งที่ปิดไปแล้ว
· แถบที่เปิดหลังเลื่อนเดือนดูจากว่ากิจกรรมนั้นอยู่ลิสต์ไหน ไม่ต้องส่งชื่อแถบมาทาง URL

> กิจกรรมหลายวันที่คร่อมสัปดาห์จะมี **แถบหลายท่อน** (`clip-l`/`clip-r`)
> ทุกท่อนผูก id เดียวกัน ตอนเลือกจึงเน้นพร้อมกันหลายท่อน — ถูกต้องแล้ว ไม่ใช่บั๊ก

นิยามของทั้งสองลิสต์ยืมมาจาก `act.today_groups()` + `act.upcoming()` ชุดเดียวกับหน้า
`/next` **ห้ามเขียน query ใหม่ที่นี่** ไม่งั้นสองหน้าจะนิยาม "งานค้าง" ไม่ตรงกัน

### 12f. ไฟล์ static ต้องต่อเลขเวอร์ชันเสมอ
`view.asset()` ต่อ `?v=<mtime>` ให้ CSS/JS ทุกไฟล์ · **ห้ามเขียน `/static/...` ตรงๆ
ในเทมเพลตอีก** ใช้ `{{ asset('css/app.css') }}` แทนเสมอ

ถ้าไม่ทำ เบราว์เซอร์จะใช้ของที่แคชไว้ต่อไปแม้เราแก้ไฟล์แล้ว แล้วเกิดอาการที่
**หาสาเหตุยากมาก** — HTML เป็นของใหม่ (หน้าเว็บไม่ถูกแคช) แต่ CSS/JS เป็นของเก่า
ผลคือปุ่มใหม่ขึ้นแบบไม่มีสไตล์ และตัวจัดการคลิกไม่ตรงกับ markup จนหน้าจอว่างเปล่า
โดยไม่มี error ให้เห็น · เกิดจริงตอนเปลี่ยนแผงข้างของหน้าปฏิทิน (2026-08-27)
ผู้ใช้เห็นแถบสองอันแต่ไม่มีข้อมูลเลย ทั้งที่เซิร์ฟเวอร์ส่งข้อมูลมาครบ

อ่าน mtime สดทุกครั้ง ไม่แคชไว้ในตัวแปร เพราะระหว่างแก้งานเรารีสตาร์ตเฉพาะเซิร์ฟเวอร์บ้าง
ไม่ได้รีสตาร์ตทุกครั้งที่แก้ไฟล์ static

### 12g. พารามิเตอร์ตัวกรองใน URL ต้องใช้ `forms.OptInt`
**ห้ามประกาศเป็น `int | None` ตรงๆ** — ช่องเลือก "ทุกคน / ทุกประเภท" ในฟอร์มตัวกรอง
ส่งมาเป็น `""` ไม่ใช่ค่าว่างจริง FastAPI จะแปลงเป็น int ไม่ได้แล้วตอบ 422
พร้อม **JSON ดิบเต็มหน้าจอ** ผู้ใช้เห็นข้อความอังกฤษยาวๆ โดยไม่รู้ว่าเกิดอะไรขึ้น

เจอจริง 2026-08-27 · เป็นบั๊กที่มีมาตั้งแต่เขียนตัวกรอง ไม่ใช่ของใหม่ —
กระทบ 6 จุดใน 3 หน้า (`/calendar` · `/next` · `/history` + export)

`OptInt` แปลงทั้งค่าว่างและค่าที่เพี้ยนเป็น `None` = "ไม่กรอง" ตามหลักการเดียวกับ
`thaidate.parse_iso()` — ค่าพวกนี้มาจาก URL ที่ผู้ใช้พิมพ์เองหรือบุ๊กมาร์กเก่าได้
การไม่กรองให้ดีกว่าการโยนหน้า error ใส่คนที่แค่อยากดูตาราง

เพิ่มหน้าใหม่ที่มีตัวกรองเมื่อไร ให้ใช้ `OptInt` เสมอ ประกาศไว้ที่ `forms.py` ที่เดียว

### 12h. กิจกรรมที่ไม่ต้องกดเริ่ม (needs_start, migration 010)
บางรายการเป็นแค่การแจ้งให้ทราบ ไม่มีใครต้องลงมือทำ เช่น "พนักงานลาหยุด"

**ค่าตั้งต้นอยู่ที่ทะเบียนประเภท (`activity_categories.needs_start`)
ห้ามฝังชื่อประเภทไว้ในโค้ด** — ถ้าฝัง พอผู้ใช้เปลี่ยนชื่อประเภทหรือเพิ่มประเภท
แจ้งเตือนใหม่ ค่าตั้งต้นจะเงียบหายไปโดยไม่มีอะไรเตือน · ประเภทเป็นแค่ค่าตั้งต้น
ตอนกรอก ค่าจริงอยู่ที่ `activities.needs_start` ซึ่งคนกรอกเปลี่ยนรายตัวได้เสมอ

`needs_start = FALSE` มีผล 4 อย่าง **ต้องครบทั้งสี่** ไม่งั้นจะเพี้ยนคนละแบบ:

| ที่ไหน | ทำอะไร |
|--------|--------|
| `start()` | ปฏิเสธที่เซิร์ฟเวอร์ ไม่ใช่แค่ซ่อนปุ่ม |
| `_SELECT.days_late` | คืน 0 เสมอ |
| `today_groups()` | ไม่เข้ากลุ่มงานค้าง (และตัดออกจากเงื่อนไขกลุ่มวันนี้ให้ตรงกัน) |
| `scheduler.close_stale()` | ข้ามไป |

ที่ต้องกันออกจาก "งานค้าง" เพราะมันจะไม่มีวันถูกกดเริ่ม ถ้าไม่กัน
มันจะกองสะสมตลอดไปจนบังงานจริงที่ต้องทำ

**ยังต้องขึ้นบนปฏิทินและในลิสต์งานต่อไปตามปกติ** — มันคือข้อมูลที่คนต้องเห็น
แค่ไม่ต้องกดอะไรกับมัน

`scheduler._insert_occurrence()` สืบค่าจากประเภทของแผนงานประจำด้วย `COALESCE(SELECT ...)`
ไม่งั้นแผนประจำที่เป็นการแจ้งเตือนจะกลายเป็นงานค้างทุกเดือน

ฟอร์มจำว่าผู้ใช้แตะช่องนี้เองหรือยัง (`touched`) — เปลี่ยนประเภททีหลัง
ต้องไม่ทับค่าที่เขาตั้งใจเลือก · ตอนแก้ไขงานเดิมถือว่าตั้งใจไว้แล้วเสมอ

### 12i. ถอดเมนู "งานต่อไป" · ปุ่มลบในกล่องข้าง
เมนู `/next` ถูกถอดออกตามที่ผู้ใช้สั่ง — กล่องข้างของปฏิทินมีทั้งงานต่อไปและงานค้างแล้ว

**ป้ายจำนวนงานค้างย้ายมาไว้ที่เมนูปฏิทิน** ห้ามลืมข้อนี้ ไม่งั้นถอดเมนูเก่าออกแล้ว
จะไม่เหลือที่ไหนเตือนว่ามีงานค้างอยู่เลย

**ตัวหน้า `/next` กับ `templates/next.html` ยังอยู่ในโค้ด** แค่ไม่มีลิงก์ในเมนู
(ตอนถอดเมนู โปรเจกต์ยังไม่อยู่ใน git จึงไม่กล้าลบ · ตอนนี้อยู่ใน git แล้ว —
`https://github.com/rapeepatfarm/rpf_calendar` tag `version1` — จะลบก็ย้อนได้)
ถ้าจะเอาเมนูคืน แก้ `templates/base.html` สองจุด (sidebar + tabbar) พอ

หน้าแรกหลังล็อกอินและ `/` `/today` `/upcoming` ชี้ไป `/calendar` หมดแล้ว
`"/next"` ยังอยู่ใน `SAFE_PREFIXES` และ `MAIN_PAGES` โดยตั้งใจ เผื่อมี back_url
ที่ค้างอยู่ใน session ของผู้ใช้เดิม

**ปุ่มลบ** อยู่ทั้งหน้ารายละเอียดและกล่องข้างของปฏิทิน · `soft_delete()` ตั้ง
`is_deleted = TRUE` ไม่ลบแถวจริง ประวัติจึงอยู่ครบ · สิทธิ์คือ `can_manage()`
(หัวหน้างานขึ้นไป) **เช็คที่ router ด้วยเสมอ ไม่ใช่แค่ซ่อนปุ่ม** เพราะยิง POST ตรงได้
— ทดสอบแล้ว worker/viewer ได้ 403 ทั้งที่ปุ่มไม่ขึ้นบนจอ

`delete()` กลับไปหน้าที่กดมาผ่าน `_safe_next(next, "/calendar")` ไม่โยนไปหน้าอื่น
ให้ผู้ใช้หลงทาง · `_safe_next` กันปลายทางนอก `SAFE_PREFIXES` อยู่แล้ว

### 12j. ทะเบียนพนักงาน — ฝ่าย + รหัส (v2 เฟส A, migration 011)
ตาราง `staff` เดิม (เมนูเคยชื่อ "ผู้รับผิดชอบ") ขยายเป็นทะเบียนพนักงานทั้งฟาร์ม
เมนูชื่อ **"จัดการพนักงาน"** · คำว่า "ผู้รับผิดชอบ" ยังใช้กับ*ช่อง* `assignee_id`
ในกิจกรรมเหมือนเดิม อย่าเปลี่ยนป้ายช่องนั้นตาม

- **`departments` เป็นตาราง ไม่ใช่ข้อความอิสระ** เพราะต้องกรองและ "เลือกทั้งฝ่าย"
  เป็นผู้ปฏิบัติงาน (เฟส D) · ส่วน `position` ยังเป็นข้อความอิสระ ตัวกรองดึงจาก
  `DISTINCT` ของค่าที่กรอกไว้จริง และฟอร์มมี `<datalist>` ให้เลือกซ้ำ
- **ฝ่ายกับจุดงานเป็นคนละชั้น** — ฝ่ายเลี้ยงไก่มีจุดงานโรงเรือน A/B/C · จุดงานมาในเฟส C
  (`duty_posts.department_id`) · ห้ามเอาโรงเรือนไปเป็นฝ่าย
- **`staff.code` เป็นกุญแจข้ามโปรแกรม ไม่ใช่กุญแจภายใน** — ภายในผูกด้วย `staff.id`
  เหมือนเดิม · ว่างได้ (คนเดิมยังไม่มีรหัส) แต่กรอกแล้วห้ามซ้ำ จึงเป็น partial unique
  index `staff_code_uq WHERE code <> ''` · router เก็บเป็นตัวพิมพ์ใหญ่เสมอ
- `save_staff()` ต้องแยก `UniqueViolation` ด้วย `e.diag.constraint_name` เพราะตารางนี้
  มี unique สองตัว (`staff_code_uq` / `staff_user_id_key`) ถ้าไม่แยก รหัสซ้ำจะขึ้น
  ข้อความว่า "บัญชีถูกผูกกับคนอื่น" ซึ่งผิดเรื่อง
- หน้าฝ่าย `/master/departments` **ไม่มีเมนูของตัวเอง** เข้าจากปุ่มบนหน้าพนักงาน
  (กติกา 12c ยังครบเพราะมีทางเข้า) · เมนู "จัดการพนักงาน" ติดสว่างทั้งสอง path
- ตัวกรองบนหน้าพนักงานส่งกลับเป็นช่องซ่อน `f_department_id` / `f_position` ในฟอร์มบันทึก/ลบ
  แล้ว `_back(keep=...)` ต่อกลับลง URL — ไม่งั้นกรองดูฝ่ายเดียวอยู่ แก้คนหนึ่งเสร็จ
  จะเด้งกลับไปเห็นทุกฝ่ายรวมกัน
- ในเทมเพลตหน้าพนักงาน ตัวกรองชื่อ `flt` ไม่ใช่ `f` — `f` เป็นฟอร์มของ Alpine อยู่แล้ว

ลบฝ่ายที่ยังมีคน / ลบคนที่ยังมีงานหรือมีวันลา → ปิดใช้งานแทนการลบ (แบบเดียวกับประเภทกิจกรรม)

### 12k. วันลาเป็นตารางของตัวเอง (v2 เฟส B, migration 012)
การลา**ไม่ใช่กิจกรรมอีกต่อไป** — อยู่ที่ `staff_leaves` (+ ทะเบียน `leave_types`)
ประเภทกิจกรรม "พนักงานลาหยุด" ถูกปิดใช้งาน และรายการเดิมถูกย้ายมา (ดู `moved_from_activity_id`)

**นิยาม "ใครลา / ใครมาทำงาน" อยู่ใน `services/staffing.py` ที่เดียว** ห้ามเขียน query
เรื่องการลาที่อื่น — บทเรียนเดียวกับงานค้าง · เฟส C เติม "ประจำจุด / ขาดคน" ลงไฟล์เดียวกัน
· เฟส D ใช้ `on_leave_ids()` กันเลือกคนลาเป็นผู้ปฏิบัติงาน

- **ยังขึ้นเป็นแถบบนปฏิทินเหมือนเดิม** (ผู้ใช้ขอ §29.4) ผ่าน `staffing.bar_items()` ที่แปลงเป็น
  pseudo-item ให้ `calgrid.build_weeks()` — ติดธง `kind='leave'` เทมเพลตวาดคนละแบบ
  (ลายทแยง สีประจำตัวคน) กดแล้ว `pickStaffDay()` เปิดแถบพนักงาน ไม่ใช่รายละเอียดกิจกรรม
  · id เป็นเลขติดลบกันทับ id กิจกรรมใน `_sort_key`
- **แถบลาได้ชั้นต่างหาก ไม่กินโควตาของกิจกรรม** — `lane_rank=0` ทำให้มาก่อนใน `_sort_key`
  และ `build_weeks()` ให้กิจกรรมได้ `MAX_LANES` นับจากชั้นที่การลาใช้ไป · ถ้าไม่ทำ วันวัคซีน
  4 รายการจะกินครบ 4 ชั้นแล้วแถบลาหลุดเข้า "+N" (เจอจริง 17–20 ก.ย. 69) ทั้งที่การลา
  ไม่มีที่อื่นให้เห็นนอกจากแถบ · การลาเองมีเพดาน `MAX_LEAVE_LANES = 3` กันสัปดาห์ที่ลากันหลายคน
- **การลาต้องไม่ปนเข้า `items`** ที่ส่งไปลิสต์งานต่อไป/งานค้าง — ส่ง `leaves_json` แยก
  · ตัวกรองผู้รับผิดชอบใช้กับการลาด้วย ตัวกรองประเภท/สถานะไม่เกี่ยว
- **แถบ "พนักงาน" ในกล่องข้างผูกกับวันที่กด** (ไม่เลือก = วันนี้) ต่างจากสองแถบแรก ·
  ส่งทะเบียนคน + รายการลาทั้งเดือนไปทีเดียว ให้ JS แยก ลา/มาทำงาน เอง (เทียบ ISO เป็นข้อความ)
  เบากว่าส่งรายวัน 42 วัน × ทุกคน · ชื่อวันไทยส่งเป็น `day_labels` — JS ห้ามจัดรูปวันไทยเอง
  · `?tab=staff` มีไว้ให้กลับจากหน้าวันลาแล้วเปิดแถบนี้ต่อ (`leaveAddUrl`)
- **ครึ่งวันใช้ได้เฉพาะแถวที่เป็นวันเดียว** (`staff_leaves_half_day_chk`) ลา 4.5 วัน =
  แถว 4 วัน + แถวครึ่งวัน · ฟอร์มซ่อนช่องครึ่งวันเมื่อเป็นหลายวัน แต่เซิร์ฟเวอร์เช็กซ้ำใน `validate()`
- **คนเดียวกันลาซ้อนช่วงไม่ได้** (`overlap()` ตัด `is_deleted` และตัดตัวเองตอนแก้ไข)
  ครึ่งเช้า + ครึ่งบ่ายวันเดียวกันก็ถือว่าซ้อน ให้บันทึกเป็นทั้งวัน
- ลบแบบซ่อน (`is_deleted`) — วันลาคือประวัติ รายงานวันลาในอนาคตต้องนับได้
- สิทธิ์: manager ขึ้นไปทั้ง `/leaves` และ `/master/leave-types` (ผู้ใช้ยืนยัน) · worker
  เห็นแถบลาและแถบพนักงานได้ แต่ปุ่ม "จัดการ/บันทึกการลา" ผูก `canManage`

**บทเรียนตอนทำ: เทมเพลตโหลดใหม่เองทันที แต่ router ต้องรีสตาร์ต** — เซิร์ฟเวอร์จริงรันโดย
RPF Console ด้วยสิทธิ์ Admin ปิดจากเชลล์ไม่ได้ ช่วงรอผู้ใช้กดรีสตาร์ต เทมเพลตใหม่จะเจอ router เก่า
· ตัวแปรใหม่ที่ส่งเข้าเทมเพลตหลัก (ปฏิทิน) จึงเขียนแบบ `(staff_json or [])` ให้หน้ายังเปิดได้
ระหว่างนั้น · หน้าจัดการพนักงานเคยพังไปช่วงหนึ่งเพราะไม่ได้ทำแบบนี้ (2026-09-16)

### 13. บังคับเปลี่ยนรหัสผ่าน
`users.must_change_password` — บังคับใน `auth.require_login()` ที่เดียว
เพราะทุกหน้าเรียกผ่านฟังก์ชันนี้ จึงไม่มีทางลืมหน้าใดหน้าหนึ่ง
· ต้องยกเว้น `/account` `/logout` `/login` `/static` ไม่งั้นผู้ใช้ติดวงวนไม่มีทางออก
· ผู้ดูแลสร้างบัญชี/ตั้งรหัสให้ = ตั้งธงเป็น TRUE เสมอ (รหัสที่คนอื่นตั้งให้ถือเป็นรหัสชั่วคราว)

### 14. เวลา
`planned_date` เป็น `DATE` ล้วน · เวลาเป็น `TIME` ล้วน · `started_at`/`finished_at`
เป็น `TIMESTAMPTZ` · ใช้ `thaidate.today()` เสมอ **ห้ามใช้ `date.today()`**
เพราะเซิร์ฟเวอร์อาจตั้ง timezone อื่นแล้ววันจะเพี้ยนไป 1 วัน
`THAI_TZ` เป็น offset UTC+7 คงที่ ไม่ใช้ `ZoneInfo` (Windows ไม่มี tzdata)

---

## โครงสร้างไฟล์

```
main.py              FastAPI app + lifespan
config.py            DB_CONFIG, SESSION_SECRET, THAI_TZ, ONLINE
database.py          connection pool + fetchall/fetchone/execute
auth.py              bcrypt, session, สิทธิ์ 4 ระดับ
forms.py             แปลงค่าจากฟอร์ม ("" → None)
view.py              Jinja2 กลาง + globals + ป้ายงานค้างข้างเมนู
routers/             auth · next_work · calendar · activities · plans · history · master · users
                     · leaves (v2 วันลา)   (/settings เป็นหน้ารวมทางเข้า อยู่ใน main.py)
services/
  activities.py      ตรรกะกิจกรรมทั้งหมด + ที่เดียวที่เขียน activity_log
  staffing.py        (v2) นิยาม ใครลา/ใครมาทำงาน ที่เดียว + แปลงการลาเป็นแถบปฏิทิน
  recurrence.py      กติกาทำซ้ำ -> รายการวัน + คำบรรยายไทย (เขียนเอง ไม่ใช้ dateutil)
  scheduler.py       งานเบื้องหลังรายวัน — สร้างล่วงหน้า + ปิดงานค้างเกินกำหนด
  calgrid.py         จัดวางแถบลงตารางเดือน/สัปดาห์ (ตัดตามสัปดาห์ + จัดชั้น)
  reports.py         สถิติย้อนหลัง — นิยาม % ทำตรงแผน อยู่ที่นี่ที่เดียว
  loginguard.py      จำกัดการเดารหัสผ่าน (in-memory ต่อโปรเซส)
  thaidate.py        จัดรูปวันที่ไทย (พ.ศ.) และ add_months ที่หนีบวันสิ้นเดือน
sync/                ซิงค์จากโปรแกรมพี่น้อง
  base.py            ExternalActivity + SyncSource protocol
  runner.py          upsert + กติกาความปลอดภัย 6 ข้อ
  sources/farm_activities.py   แผนกิจกรรมฝูงไก่จาก rpf_farm (แหล่งจริงตัวแรก)
  sources/_example.py แม่แบบสำหรับเขียน adapter ตัวต่อไป
migrations/          001_init · 002_sync · 003_multiday · 004_helpers
                     · 005_force_password_change · 006_anchor_mode · 007_date_override
                     · 008_start_shifts_plan · 009_shift_source · 010_needs_start
                     · 011_staff_registry (v2 เฟส A) · 012_leaves (v2 เฟส B)
scripts/             create_user.py · new_secret.py · seed_master.py · show_urls.py
                     · serve_testdb.py (เปิดสำเนาชี้ DB ทดสอบบนพอร์ต 8201 ไว้ตรวจหน้าจอ)
tests/               pytest — ตรรกะล้วน ไม่แตะ DB
```

---

## สิทธิ์ 4 ระดับ

| role | สร้าง/แก้/ยกเลิก | กดเริ่ม-จบ | master | users |
|---|---|---|---|---|
| `admin` | ทุกงาน | ทุกงาน | ✔ | ✔ |
| `manager` | ทุกงาน | ทุกงาน | ✔ | ✘ |
| `worker` | เฉพาะงานที่ตัวเองสร้างและยังไม่เริ่ม | **เฉพาะงานของตัวเอง** | ✘ | ✘ |
| `viewer` | ✘ | ✘ | ✘ | ✘ |

`worker` รู้ว่างานไหน "ของตัวเอง" จาก `staff.user_id` ที่ผูกไว้
`owns_activity()` ต้องเช็ก `staff_id is not None` ก่อน ไม่งั้น `None == None`
จะทำให้ทุกคนเป็นเจ้าของงานที่ยังไม่มอบหมาย

ธง `can_run` / `can_edit` ติดไปกับข้อมูลผ่าน `act.annotate(rows, user)`
template จึงไม่ต้องรู้กฎสิทธิ์เอง

---

## สถานะที่ทำแล้ว / ยังไม่ทำ

**เสร็จแล้ว (เฟส 0–4 + โครง sync)**
ล็อกอิน 4 สิทธิ์ · master ประเภท/ผู้รับผิดชอบ · สร้าง-แก้-ลบกิจกรรม ·
**งานหลายวัน** แสดงเป็นแถบต่อเนื่องในปฏิทิน · หน้า **งานต่อไป** (รวมงานวันนี้กับงานที่จะถึง) ·
ปฏิทินรายเดือน + แผงรายละเอียดรายวันที่เน้นแถบที่เลือก · วงจรสถานะครบ + timeline ประวัติ ·
**แผนงานประจำ** (รายเดือน/รายปี/รายสัปดาห์/รายวัน) + ตัวสร้างล่วงหน้าอัตโนมัติ ·
`auto_skip_after_days` ทำงานแล้วผ่าน scheduler · **ยืนยันก่อนเริ่มงาน + ถอนการเริ่ม** ·
**มุมมองรายสัปดาห์** · **หน้าประวัติและสรุป** (% ทำตรงแผน · ภาระงานรายคน · รายประเภท) ·
**ส่งออก Excel 3 ชีต** · **ผู้ช่วยหลายคนต่องาน** · **ตัวกันเดารหัสผ่าน** · โครง sync

**เฟส 5 — ซิงค์แผนกิจกรรมฝูงไก่จาก RPF Farm (เสร็จแล้ว)**

| ส่วน | ไฟล์ |
|------|------|
| adapter | `sync/sources/farm_activities.py` |
| หน้าจัดการ | `/settings/sync` — `routers/sync_admin.py` + `templates/settings_sync.html` |
| การเชื่อมต่อ | `FARM_DB_CONFIG` ใน `config.py` + `get_farm_conn()` ใน `database.py` |

- ใช้บัญชี PostgreSQL **`rpf_readonly`** (SELECT อย่างเดียว) กติกาข้อ 4 จึงถูกบังคับโดย DB จริง
  ไม่ใช่แค่ความตั้งใจของคนเขียนโค้ด — โค้ดที่เผลอ UPDATE จะพังทันทีให้เห็น
- `external_id = flock_activity_plans.id` (เลขนิ่ง) — ฝั่งฟาร์มเปลี่ยนโปรแกรมกลางคัน
  แผนเก่าจะถูกตั้ง `is_deleted` แล้วหลุดจาก `fetch()` runner จึงปิดเป็น `cancelled` ให้เอง
  ไม่ต้องเขียนโค้ดลบเพิ่ม
- แผนที่ฟาร์มระบุว่ากินเวลาหลายวัน (`duration_days`) มาเป็น `ExternalActivity.end_date`
  แล้วกลายเป็นแถบต่อเนื่องตามกติกาข้อ 6 · `fetch()` จึงต้องคืนแบบ **คาบเกี่ยวช่วง**
  ไม่ใช่ `planned_date >= since` เฉยๆ ไม่งั้นงานหลายวันที่เริ่มก่อน since แต่ยังทำอยู่
  จะหลุดจากชุดที่คืนมา แล้ว runner จะปิดเป็น cancelled ทั้งที่ยังทำค้างอยู่
- ผลที่ทำจริงไม่ได้ push กลับ — **ฟาร์มเป็นฝ่ายมาดึงเอง** จาก `activities.finished_at`
  (ปุ่มอยู่ในหน้า `/calendar` ของ rpf_farm) ปฏิทินจึงยังไม่เขียนข้าม DB ตามกติกาข้อ 4
- **วันที่ที่ถูกแก้ที่นี่ก็ไหลกลับทางเดียวกัน** — ฟาร์มดึงทั้งผลงานและวันที่แก้ในปุ่มเดียว
  (`pull_results()` + `pull_date_changes()`) ดูกติกาข้อ 12

> **บั๊กที่แก้ไปตอนต่อ adapter ตัวแรก:** `runner._insert()` ไม่ได้ใส่ `planned_end_date`
> ซึ่งถูกเพิ่มมาใน `003_multiday.sql` — โครง sync เขียนไว้ก่อนหน้านั้นและไม่เคยถูกรันจริง
> พร้อมกันนั้น `activities.finish()/cancel()` ก็ยังไม่รองรับ `user=None` ทั้งที่ runner
> ประกาศรับได้ (ตอนรันอัตโนมัติไม่มีคนกด) — แก้ให้รับ None เหมือน `_log()` แล้ว

**Version 2 — จัดสรรพนักงาน (ข้อตกลงอยู่ที่ §29 ของแผน · โค้ด v1 คือ tag `version1` บน GitHub)**

| เฟส | สถานะ |
|---|---|
| A ทะเบียนพนักงาน — ฝ่าย · รหัส · ตัวกรอง · เมนู "จัดการพนักงาน" | **เสร็จ** (migration 011 ลงจริงแล้ว 2026-09-16) |
| B วันลา — `leave_types` · `staff_leaves` (ครึ่งวันได้) · แถบลาบนปฏิทิน · แถบพนักงานในกล่องข้าง · ย้าย "พนักงานลาหยุด" ออกจากกิจกรรม | **เสร็จ** (migration 012 · ดู 12k) |
| C หน้าที่ประจำ — `duty_posts` · `duty_assignments` (คนแทน = แถวที่มี `covers_staff_id`) · ตรวจขาดคนตอนบันทึกลา · ป้ายเลขที่เมนู | ยังไม่ทำ |
| D ผู้ปฏิบัติงานในกิจกรรม = `activity_helpers` เดิม · เลือกทั้งฝ่าย · ลา = ล็อก / อื่นๆ = เตือน | ยังไม่ทำ |

**ยังไม่ทำ**
- ตั้งเวลาให้ซิงค์อัตโนมัติผ่าน `scheduler.py` (ตอนนี้กดเองที่ `/settings/sync`)
- มุมมองรายวันแยก (ตอนนี้ใช้แผงรายละเอียดในปฏิทินแทน ซึ่งพอสำหรับงานฟาร์ม)
- แก้แผนงานประจำตอนนี้เป็นแบบ "ตั้งแต่นี้ไป" อย่างเดียว ยังไม่มีตัวเลือก "แก้เฉพาะครั้งนี้"
  จากหน้าแผนงาน (แต่แก้กิจกรรมรายครั้งจากหน้ากิจกรรมได้อยู่แล้ว)

## หมายเหตุการใช้งานจริงของฟาร์มนี้
ผู้ใช้ยืนยันว่าแผนงานประจำ **ส่วนมากเป็นรายเดือนและรายปี** ส่วนรายสัปดาห์ตั้งใจไม่ค่อยใช้
เพื่อกันกิจกรรมล้นปฏิทิน · ตัวเลือกในฟอร์มจึงเรียงรายเดือน/รายปีขึ้นก่อน
และ `recurrence.py` ให้ความสำคัญกับความถูกต้องของสองแบบนั้นเป็นอันดับแรก
